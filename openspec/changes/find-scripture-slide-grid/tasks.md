## 1. Failing tests (TDD)

- [x] 1.1 Add chrome tests that Find Scripture hugs and wraps like the Scripture grid, and a topical VM test that `SetCardPaneWidth` stamps `CardWidth` on results and a wider pane grows the cards. Verify they fail

## 2. Policy and VM

- [x] 2.1 Add Find Scripture hug/wrap chrome flags. Add `SetCardPaneWidth` + stamp on add in `TopicalSearchViewModel`. Verify task 1.1 is green

## 3. Find Scripture UI

- [x] 3.1 Bind topical cards to `CardWidth`, top-align items, update width from topical list and window resize. Verify the window builds
