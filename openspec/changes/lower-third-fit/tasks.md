## 1. Failing tests (TDD)

- [x] 1.1 Add tests: short verse on default Lower Third is one slide; a body taller than the box becomes 2+ slides each within the usable box; regions clip. Verify they fail

## 2. Pagination and clip

- [x] 2.1 Share usable-box math, keep/fix `DeckBuilder` paging, clip region grids. Verify task 1.1 is green
