## 1. Failing tests (TDD)

- [x] 1.1 Add `OperatorWorkspaceChromeTests` for one-row toolbar, no inner title bar, title bubble colors, and hide-on-Songs-tab. Verify the test fails because those chrome members do not exist

## 2. Chrome policy

- [x] 2.1 Add toolbar-in-tab-strip, no-inner-title-bar, title-is-bubble, bubble colors, and hide-on-Songs-tab on `OperatorWorkspaceChrome`. Verify task 1.1 passes

## 3. Operator UI

- [x] 3.1 Move title + Follow + Lines + add-slide + size onto the Songs tab strip (far right, title as a pink bubble). Remove the inner Now Singing title bar. Verify the cluster is visible only when Now Singing has a song, and the window builds
