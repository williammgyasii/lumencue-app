## 1. Failing tests (TDD)

- [x] 1.1 Add `SttOperatorCopyTests` that engine label is `Transcription agent` and connecting copy is `Connecting to transcription agent...`, with no `ElevenLabs` / `Scribe` / `Deepgram`. Verify they fail

## 2. Copy

- [x] 2.1 Add `SttOperatorCopy` and point ElevenLabs + Deepgram operator strings at it. Verify task 1.1 is green
