{
  "name": "lumencue-app",
  "private": true,
  "scripts": {
    "dev:api-edge": "wrangler dev",
    "deploy:api": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "wrangler": "^4.120.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.31"
  }
}
